{
  "name": "30-days-js",
  "version": "1.0.0",
  "description": "First Steps with js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-compile": "node-sass --watch sass -o css --source-map true",
    "devserver": "live-server --port=3030 -q",
    "start": "npm-run-all --parallel devserver sass-compile"
  },
  "author": "Kae",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
